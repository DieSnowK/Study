#include <iostream>
#include <cstdlib>
#include <sys/types.h>
#include <signal.h>
int main()
{
    // raise(2);
    // sleep(100);

    // raise(SIGINT);
    // sleep(100);

    // abort();
    // sleep(100);

    return 0;
}